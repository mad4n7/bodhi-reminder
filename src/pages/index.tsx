import { Meta } from '@/layouts/Meta';
import { Main } from '@/templates/Main';

const Index = () => {
  return (
    <Main meta={<Meta title="BodhiReminder" description="First version" />}>
      <h1>Hi there</h1>
    </Main>
  );
};

export default Index;
